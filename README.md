# cloning the repository
@todo write command for git submodules

## init content
for some large files git LFS is used.

for Fedora:
```bash
# Install Git LFS
sudo dnf install git-lfs

# Initialize Git LFS in your repository
git lfs install

# Pull the actual file contents
git lfs pull
```

# Algorithms & Data Structures — personal knowledge base

This repo is my long-term storage for interview prep:
- **Roadmap / order**: see `00_roadmap/`
- **Knowledge base (one-page notes)**: see `10_kb/`
- **Practice tracker (what/when/next)**: see `30_registry/`

Existing table of contents + debugging/pitfalls (legacy but valuable):
- `__MAIN__/README.md`

## Quick start (pick one)
- If you have **30–45 min**: do 1 problem from “Revisit”, then 1 new problem from Core Track.
- If you have **60–90 min**: do 1 timed problem set (see `20_practice/`), then update the tracker.

Next: open `00_roadmap/core-track.md`.